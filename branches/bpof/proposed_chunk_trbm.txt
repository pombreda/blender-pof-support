char[4] chunk_id = "TRBM"   // tractor beam
int length
uint num_emitters            // number of tractor beam emitters
for each emitter, i {
    uint sobj_id[i]         // parent submodel
    vector3d location[i]    // location, relative to the submodel
    vector3d normal[i]      // normal
    uint fov[i]             // field of view
    
Strength is controlled by ships.tbl entry, SEXPs, and armor.  Texture/color/etc. controlled by species_defs.tbl entry.